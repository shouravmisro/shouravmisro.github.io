---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Container from "../../components/Container.astro";
import Button from "../../components/Button.astro";
import { loadSiteJson } from "../../lib/sitejson";

const data = loadSiteJson();
const b = data.basics;

const iotSkills = data.skills.filter((s) =>
  [
    "ESP32",
    "Arduino",
    "Raspberry Pi",
    "Embedded C",
    "Sensor Interfacing",
    "Actuator Control",
    "GPIO / ADC / PWM / Timers",
    "UART / I2C / SPI",
    "MQTT / HTTP REST",
    "Firmware Debugging",
    "System Validation"
  ].includes(s.name)
);

const qaSkills = data.skills.filter((s) =>
  [
    "Manual Testing",
    "Test Case Design",
    "Bug Reporting",
    "API Testing (Postman)",
    "Regression Testing",
    "Jira"
  ].includes(s.name)
);

const iotProjects = data.projects.filter((p) =>
  p.stack.some((s) =>
    ["IoT", "Embedded", "ESP32", "Arduino", "Raspberry Pi", "Firmware"].includes(s)
  )
);

const qaProjects = data.projects.filter((p) =>
  p.stack.some((s) =>
    ["QA", "Manual Testing", "Functional Testing", "Regression Testing", "Postman"].includes(s)
  )
);
---

<BaseLayout title={`IoT Resume — ${b.name}`} description={`IoT & Embedded Systems Engineer Resume and profile of ${b.name}`}>
  <section class="py-10 min-h-[100dvh]">
    <Container>
      <div class="grid gap-6 lg:grid-cols-[300px_1fr]">
        <!-- Left sticky sidebar -->
        <aside class="lg:sticky lg:top-20 h-max">
          <div class="gsap-resume-slide-left card p-5 relative overflow-hidden transition-all duration-500 hover:border-[rgba(var(--fg)/0.2)]">
            {b.headshotUrl && (
              <img
                src={b.headshotUrl}
                alt={`${b.name} headshot`}
                class="h-24 w-24 rounded-full border border-[rgb(var(--border))] object-cover"
              />
            )}

            <h1 class="mt-4 text-2xl font-extrabold">{b.name}</h1>
            <p class="mt-2 text-sm text-[rgb(var(--fg)/0.70)]">IoT &amp; Embedded Systems Engineer</p>

            <div class="mt-4 space-y-2 text-sm text-[rgb(var(--fg)/0.78)]">
              <div>{b.email}</div>
              <div>{b.phone}</div>
              <div>{b.location}</div>
            </div>

            <div class="mt-5 grid gap-2">
              <Button href={b.resumeIotUrl ?? "/resume-iot.pdf"} target="_blank">Download IoT CV</Button>
              <Button href={b.resumeSqaUrl ?? "/resume-sqa.pdf"} variant="secondary" target="_blank">
                Download SQA CV
              </Button>
            </div>

            <div class="mt-5 border-t border-[rgb(var(--border))] pt-4">
              <div class="text-xs font-semibold uppercase tracking-wide text-[rgb(var(--fg)/0.55)]">
                Primary Focus
              </div>

              <div class="mt-3">
                <div class="font-semibold text-sm">IoT / Embedded Systems</div>
                <p class="mt-1 text-sm text-[rgb(var(--fg)/0.72)]">
                  ESP32, Arduino, Raspberry Pi, communication protocols (MQTT, I2C, SPI), sensor integration, and firmware debugging.
                </p>
              </div>
            </div>
          </div>
        </aside>

        <!-- Main content -->
        <div class="grid gap-4">
          <section class="gsap-resume-card card p-6 relative overflow-hidden transition-all duration-500 hover:border-[rgba(var(--fg)/0.2)]">
            <h2 class="text-xl font-bold">IoT Profile Summary</h2>
            <p class="mt-3 text-[rgb(var(--fg)/0.75)] leading-relaxed">
              I specialize in embedded firmware programming, microcontroller sensor interfacing, wireless telemetry via MQTT, and hardware system debugging. Experienced with ESP32, Arduino, Raspberry Pi, and edge decision logic.
            </p>
          </section>

          <section class="grid gap-4 md:grid-cols-2">
            <div class="gsap-resume-card card p-6 relative overflow-hidden transition-all duration-500 hover:border-[rgba(var(--fg)/0.2)]">
              <h2 class="text-xl font-bold">IoT / Embedded Skills</h2>
              <div class="mt-4 flex flex-wrap gap-2">
                {iotSkills.map((s) => (
                  <span class="rounded-full border border-[rgb(236,72,153,0.3)] bg-[rgb(236,72,153,0.06)] px-3 py-1 text-xs font-semibold text-pink-400">
                    {s.name}{s.level ? ` (${s.level})` : ""}
                  </span>
                ))}
              </div>
            </div>

            <div class="gsap-resume-card card p-6 relative overflow-hidden transition-all duration-500 hover:border-[rgba(var(--fg)/0.2)]">
              <h2 class="text-xl font-bold">Additional Software Skills</h2>
              <div class="mt-4 flex flex-wrap gap-2">
                {qaSkills.slice(0, 6).map((s) => (
                  <span class="rounded-full border border-[rgb(var(--border))] bg-[rgb(var(--bg)/0.3)] px-3 py-1 text-xs text-[rgb(var(--fg)/0.75)]">
                    {s.name}
                  </span>
                ))}
              </div>
            </div>
          </section>

          <section class="gsap-resume-card card p-6 relative overflow-hidden transition-all duration-500 hover:border-[rgba(var(--fg)/0.2)]">
            <h2 class="text-xl font-bold">IoT / Embedded Projects</h2>
            <div class="mt-4 grid gap-3">
              {iotProjects.map((p) => (
                <div class="rounded-2xl border border-[rgb(var(--border))] p-4 hover:border-[rgba(var(--fg)/0.2)] transition-colors">
                  <div class="flex items-start justify-between gap-3">
                    <div>
                      <div class="font-semibold">{p.title}</div>
                      <div class="text-sm text-[rgb(var(--fg)/0.70)]">{p.role}</div>
                    </div>
                    <div class="text-xs text-[rgb(var(--fg)/0.60)]">{p.year}</div>
                  </div>
                  <p class="mt-2 text-sm text-[rgb(var(--fg)/0.75)]">{p.summary}</p>
                </div>
              ))}
            </div>
          </section>

          <section class="gsap-resume-card card p-6 relative overflow-hidden transition-all duration-500 hover:border-[rgba(var(--fg)/0.2)]">
            <h2 class="text-xl font-bold">Experience</h2>
            <div class="mt-5 grid gap-4">
              {data.experience.map((e) => (
                <div class="rounded-2xl border border-[rgb(var(--border))] p-5 transition-colors hover:border-[rgba(var(--fg)/0.2)]">
                  <div class="flex flex-wrap items-center justify-between gap-2">
                    <div class="font-semibold text-lg">{e.title} <span class="text-[rgb(var(--fg)/0.6)] font-normal text-base">— {e.company}</span></div>
                    <div class="text-xs font-semibold px-3 py-1 bg-[rgb(var(--fg)/0.06)] rounded-full text-[rgb(var(--fg)/0.70)]">{e.start} - {e.end}</div>
                  </div>
                  <ul class="mt-3 list-disc pl-5 space-y-1 text-sm leading-relaxed text-[rgb(var(--fg)/0.75)]">
                    {e.highlights.map((h) => <li>{h}</li>)}
                  </ul>
                </div>
              ))}
            </div>
          </section>

          <section class="grid gap-4 md:grid-cols-2">
            <div class="gsap-resume-card card p-6 relative overflow-hidden transition-all duration-500 hover:border-[rgba(var(--fg)/0.2)]">
              <h2 class="text-xl font-bold">Education</h2>
              <div class="mt-4 grid gap-3">
                {data.education.map((e) => (
                  <div class="rounded-2xl border border-[rgb(var(--border))] p-4 hover:border-[rgba(var(--fg)/0.2)] transition-colors">
                    <div class="font-semibold">{e.degree}</div>
                    <div class="text-sm text-[rgb(var(--fg)/0.70)]">{e.school}</div>
                    <div class="text-xs text-[rgb(var(--fg)/0.60)] mt-1">{e.year}</div>
                  </div>
                ))}
              </div>
            </div>

            <div class="gsap-resume-card card p-6 relative overflow-hidden transition-all duration-500 hover:border-[rgba(var(--fg)/0.2)]">
              <h2 class="text-xl font-bold">Achievements</h2>
              <div class="mt-4 grid gap-3">
                {data.achievements.map((a) => (
                  <div class="rounded-2xl border border-[rgb(var(--border))] p-4 hover:border-[rgba(var(--fg)/0.2)] transition-colors">
                    <div class="font-semibold">{a.title}</div>
                    {a.org && <div class="text-sm text-[rgb(var(--fg)/0.70)]">{a.org}</div>}
                    <div class="text-xs text-[rgb(var(--fg)/0.60)] mt-1">{a.date}</div>
                  </div>
                ))}
              </div>
            </div>
          </section>
        </div>
      </div>
    </Container>
  </section>

  <script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";
    
    gsap.registerPlugin(ScrollTrigger);

    function initResumeAnimation() {
      gsap.from(".gsap-resume-slide-left", {
        opacity: 0,
        x: -30,
        duration: 0.8,
        ease: "power3.out",
        clearProps: "all"
      });

      const cards = gsap.utils.toArray(".gsap-resume-card");
      if (cards.length > 0) {
        gsap.from(cards, {
          scrollTrigger: {
            trigger: cards[0] as Element,
            start: "top 90%",
          },
          opacity: 0,
          y: 30,
          duration: 0.6,
          stagger: 0.1,
          ease: "power3.out",
          clearProps: "all"
        });
      }
    }

    initResumeAnimation();
    document.addEventListener('astro:page-load', initResumeAnimation);
  </script>
</BaseLayout>
